#include "main.h"

/**
 * add_numbers - Adds two integers
 * @a: First number
 * @b: Second number
 * Return: sum of a and b
 */
int add_numbers(int a, int b)
{
	int result;

	result = a + b;
	return (result);
}
